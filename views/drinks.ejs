<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= category %> | Bubble Tea</title>
    <link rel="stylesheet" href="/styles/menu.css">
</head>
<body>
    <header>
        <h1>🍵 Bubble Tea Shop</h1>
        <nav>
            <ul class="category-header">
                <li><a href="/menu">← Back to Categories</a></li>
            </ul>
        </nav>
    </header>

    <main>
        <h2><%= category %></h2>
        <% if (drinks.length === 0) { %>
            <p class="no-drinks">No drinks available in this category yet!</p>
        <% } else { %>
            <div class="drinks-container">
                <% drinks.forEach(drink => { %>
                    <div class="drink-item">
                        <img src="<%= drink.image_url || '/images/test.webp' %>" alt="<%= drink.drinkname %>">
                        <h3><%= drink.drinkname %></h3>
                        <p class="price">$<%= drink.price %></p>
                    </div>
                <% }); %>
            </div>
        <% } %>
    </main>
</body>
</html>
